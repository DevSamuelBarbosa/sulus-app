; Default php:8.3-fpm-alpine caps upload_max_filesize at 2M, well below the
; app's own 4096 KB (media.photo_max_kb) limit — large PNGs (e.g. transparent
; logos) were rejected by PHP before Laravel's validation rules ever ran.
upload_max_filesize = 6M
post_max_size = 8M
